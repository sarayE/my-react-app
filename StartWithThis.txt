Creat git for this folder:
============================
להכנס לתקיית הפרוייקט ולפתוח GIT
1) git init

2) git add .

3)   git config --global user.email "Saray@test.com"
     git config --global user.name "Saray"

4) git commit -m "First commit"

5) לפתוח ריפו בגיטהאב ולהעתיק את הקישור אליו

6) git remote add origin https://github.com/sarayE/my-new-repo.git

7) push -u origin master
ולהכניס שם משתמש וסיסמה

 
git status = לראות את כל הקבצים הלא מתועדים
git remote -v = לראות את כל המחשבים המקושרים
git remote rm origin  = להעיף מחשב מקושר
git commit -a -m "my string"

git reset HEAD~ = מוחק את התגובה האחרונה

כדי לעדכן את הריפו הלוקלי בגירסה שיש בשרת 
git pull




start the application:
=======================

you can dounload 'yarn'  :   https://yarnpkg.com/lang/en/docs/install/

yarn --version = לבדוק אם אכן הותקן

להכנס לתקיית הפרוייקט ולפתוח CMD
1) yarn init

2) yarn install

3) yarn start  =  ולא לסגור את החלון
הכתובת של הפרוייקט היא 
localhost:8080





